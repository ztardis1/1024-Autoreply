name: 1024-AutoReply

on:
  watch:
    types: [started]
  schedule:
    - cron: '0 19 * * *'


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: checkout actions
      run: |
        sudo apt-get update
        sudo apt-get -y install git

    - name: install env
      run :
        /usr/bin/python3 install requests
        /usr/bin/python3 install onetimepass
        /usr/bin/python3 install pillow

    - name: run 1024.py
      env:
        USER: ${{ secrets.USER }}
        PASSWORD: ${{ secrets.PASSWORD }}
        SECRET: ${{ secrets.SECRET }}
        TOKER: ${{ secrets.TOKER }}
        CODEUSER: ${{ secrets.CODEUSER }}
        CODEPASS: ${{ secrets.CODEPASS }}
      run:
        /usr/bin/python3 1024.py
